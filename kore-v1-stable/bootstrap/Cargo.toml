[package]
name = "korec"
version = "0.1.0"
edition = "2021"
description = "The Kore Compiler - Self-Hosted Bootstrap Edition"

# Project Ouroboros - The snake that eats its own tail
# This compiler was generated from Kore source code!

[workspace]

[dependencies]
inkwell = { version = "0.7.1", default-features = false, features = ["llvm21-1", "target-x86"] }
