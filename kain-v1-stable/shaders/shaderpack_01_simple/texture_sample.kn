// Simple texture sampling shader
shader fragment TextureSample(position: Vec4, uv: Vec2) -> Vec4:
    uniform base_texture: Sampler2D @0
    
    let color = sample(base_texture, uv)
    
    return color
